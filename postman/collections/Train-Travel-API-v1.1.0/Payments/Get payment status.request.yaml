$kind: http-request
description: Returns the payment status and details for a specific booking.
url: "{{baseUrl}}/bookings/:bookingId/payment"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: bookingId
    value: 1725ff48-ab45-4bb5-9d02-88745177dedb
    description: The ID of the booking to pay for.
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid",
            "default": "<uuid>"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
examples: ./.resources/Get payment status.resources/examples
order: 1000
